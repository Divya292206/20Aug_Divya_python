#Write a Python program to print every alternate character from the string starting from index 1.

text = "Hello, World!"
# Accessing every alternate character starting from index 1
alternate_characters = text[1::2]
print(f"Every alternate character from index 1: {alternate_characters}")